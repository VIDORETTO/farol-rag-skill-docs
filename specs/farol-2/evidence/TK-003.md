# TK-003 evidence — KnowledgeBackend e adapter legado

Status: `verified`.

## Delivered

- `docops.backends.base` defines the provider-neutral lifecycle
  `probe → prepare → apply → query → snapshot → discard → close` and typed,
  serializable DTOs.
- `KnowledgeRagLegacyAdapter` keeps MCP/process/vendor details behind that
  seam, supports injected deterministic retrieval, owns candidate identities,
  and never treats external dataset IDs as package identity.
- Root exports expose the seam without removing any 1.x imports or changing
  the existing `RetrievalAdapter`, `RagSynchronizer`, reader, or evaluator
  behavior.

## Executed procedures

- `uv run --isolated --python 3.11 --with pytest==8.3.5 --with PyYAML==6.0.3 --with pypdf==6.16.2 --with python-docx==1.2.0 python -m pytest -q tests/test_backend_contract.py tests/test_evaluator.py tests/test_rag_sync.py tests/test_mcp_smoke.py`
  → `33 passed, 1 skipped`; the skip is the pre-existing optional Chroma
  dependency path, not a backend contract pass.
- Same environment: `python -m pytest -q tests/test_reader_sessions.py tests/test_rag_snapshots.py tests/test_public_interface.py`
  → `17 passed`.
- `uv run --isolated --python 3.11 --with ruff==0.12.7 ruff check ...`
  → `All checks passed` for changed backend/API/test files.
- `python scripts/sync_schemas.py --check` and `python scripts/check_contracts.py --json`
  → both `ok`.

The command uses `uv` because the host has no `python` alias and its default
Python 3.11 environment lacks PyYAML. No real knowledge-rag service was
required for this adapter contract; the real RAGFlow prerequisite remains the
separate TK-001 external evidence.
