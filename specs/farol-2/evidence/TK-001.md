# TK-001 evidence — RAGFlow/DeepDoc spike

Status: `verified`; integração real RAGFlow v0.27.2, adapter Farol e cleanup
foram executados em 20 de setembro de 2026. O perfil core continua sem iniciar
Docker ou contatar serviço.

## Executed

`DOCOPS_RAGFLOW_INTEGRATION=1 uv run --isolated --python 3.13 --with 'ragflow-sdk==0.27.2' --with 'pytest==9.1.1' python -m pytest -q tests/spikes/test_ragflow_contract.py`

Result: explicit skip (`not_run: missing external RAGFlow inputs
DOCOPS_RAGFLOW_ENDPOINT, DOCOPS_RAGFLOW_TOKEN, DOCOPS_RAGFLOW_IMAGE_DIGEST,
DOCOPS_RAGFLOW_SDK_VERSION`). The pinned `ragflow-sdk==0.27.2` resolves and
imports under isolated Python 3.13; the package is not compatible with the
project's Python 3.11 core runtime. No token, endpoint, dataset ID, corpus or
secret was written to the repository.

## Contract fixed by the spike

- Expected RAGFlow/SDK version: `v0.27.2`.
- Required image identity: repository plus `@sha256:<64 hex>` digest supplied by
  the operator through `DOCOPS_RAGFLOW_IMAGE_DIGEST`.
- Required runtime inputs: endpoint, token, pinned SDK version and an operator-
  provisioned instance. Missing inputs are a not-run external prerequisite,
  not a passing hypothesis.
- Operations covered by the opt-in test: isolated dataset creation, PDF upload,
  parse, chunk retrieval, locator metadata, known-content retrieval and cleanup.

## Bloqueio histórico, superado em 2026-09-20

The required operator-provisioned RAGFlow v0.27.2 endpoint, token, pinned image
digest and service are not available in this checkout. A read-only Docker
inspection on 12 September 2026 observed an already-running
`infiniflow/ragflow:v0.27.1` container; it is not the required pinned
v0.27.2/digest input and was not started, stopped or modified. H-001/H-002
therefore remained unverified naquele momento. Esse registro foi superado pelo
run real abaixo e fica preservado apenas como histórico.

## Preflight atual do candidato `93bb889`

Em 12 de setembro de 2026, o preflight foi reexecutado em Python 3.13 com:

`uv run --isolated --python 3.13 --with-editable
.[dev,formats,ragflow] python scripts/run_ragflow_profile.py --json`

Resultado real: processo terminou com código `1`, `status=blocked`,
`reason=missing_external_inputs` e os quatro inputs ausentes:
`DOCOPS_RAGFLOW_ENDPOINT`, `DOCOPS_RAGFLOW_TOKEN`,
`DOCOPS_RAGFLOW_IMAGE_DIGEST` e `DOCOPS_RAGFLOW_SDK_VERSION`. O relatório
redigido foi `farol-v2-ragflow-preflight-998f5906fc4648cc8515b73b4666f1b4.json`,
SHA-256 `325ef82f57743904c41ee90d091962853de548c383c8cd7139096a25d9d642d`.
Como o bloqueio ocorre antes do import/probe, nenhum serviço foi contatado e
nenhum container foi iniciado.

## Preflight atual — 2026-09-18

Comando observado: `.venv\\Scripts\\python.exe scripts/run_ragflow_profile.py
--json`.

Resultado: exit code `1`, `status=blocked`, `reason=missing_external_inputs`.
Continuam ausentes `DOCOPS_RAGFLOW_ENDPOINT`, `DOCOPS_RAGFLOW_TOKEN`,
`DOCOPS_RAGFLOW_IMAGE_DIGEST` e `DOCOPS_RAGFLOW_SDK_VERSION`. O bloqueio
ocorreu antes do import do SDK e do probe; nenhum serviço, container, dataset,
token ou corpus foi tocado. A evidência permanece `not_run`, não sucesso.

## Execução real terminal — 2026-09-20

- Runtime: RAGFlow `v0.27.2`, SDK `0.27.2`, commit upstream
  `a024bea0cd93f39e6652a42bf84dd20c55bc560b`, imagem
  `infiniflow/ragflow@sha256:e6b3f1a185c0a70abb3a72e1092415df8c5c6a4fae0089545554f2936c4b27ab`.
- Dependências locais: Elasticsearch, MySQL, MinIO, Redis e TEI CPU; embedding
  `BAAI/bge-small-en-v1.5`. Portas publicadas somente em loopback.
- O primeiro parse real falhou honestamente por ausência de embedding default;
  o perfil dev foi corrigido para provisionar TEI pinado e configurar o modelo
  builtin, sem relaxar o contrato.
- `run_ragflow_profile.py --timeout 900 --json` → `status=passed`, `2 passed`,
  `0 skipped`, `0 failed` em Python 3.13.
- O spike comprovou criação de dataset isolado, upload de PDF sintético de duas
  páginas, parse terminal, chunks com locators estruturados nas páginas 1 e 2,
  retrieval do conteúdo conhecido e ausência do dataset após cleanup.
- O segundo caso exercitou o `RagFlowAdapter` real: probe, prepare, apply,
  mapping canônico, query, citação via router, snapshot, descarte, rebuild com
  novo dataset/índice e equivalência da projeção canônica após reconstrução.
- Durante a execução real foi encontrado e corrigido drift do SDK: a API
  v0.27.2 limita `document.list_chunks(page_size)` a 100; o wrapper usava 1024.
- Token, endpoint, IDs externos e texto marcador não foram persistidos nos
  receipts ou no repositório. Os datasets temporários foram removidos.

Receipt redigido e detalhes: `specs/farol-2/evidence/ragflow-spike.md`.
