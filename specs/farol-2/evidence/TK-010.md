# TK-010 evidence — taxonomia versionada

Status: `verified`; proposta, validação, aprovação e CAS foram implementados.

## Entregue

- `docops/taxonomy.py` calcula coverage, overlaps, órfãos, ownership e
  dependências, validando ciclos e referências pendentes.
- Store exige aprovação explícita, request idempotente e expected revision;
  schemas e diff preservam revisão anterior.

## Verificação

- `tests/test_taxonomy.py` passou com graph, métricas, ownership duplicado,
  aprovação, conflito CAS e idempotência.
- Contratos e documentação passaram no gate core.

### Endurecimento provider-free — 2026-09-19

- A proposta agora rejeita `slug` duplicado antes da síntese. IDs, conceitos e
  owners distintos não podem mais convergir para o mesmo diretório de skill e
  sobrescrever artefatos.
- O RED demonstrou que duas skills aprovadas com `slug=authentication` eram
  aceitas; o GREEN passou pelo seam público de `TaxonomyEngine.propose()`.

## Limites

A decisão de ownership ambígua continua exigindo revisão humana, conforme o
contrato; não há aceite local pendente neste ticket.
