# TK-006 evidence — Markdown e HTML estruturados

Status: `verified`; caminho provider-free implementado e coberto localmente.

## Entregue

- `docops/extractors/text_web.py` preserva headings, listas, tabelas, código,
  links e origem com locators determinísticos.
- Limite de blocos e classificação `structured-native`/fallback são
  aplicados antes da promoção da IR.

## Verificação

- `tests/test_text_web_extractor.py` passou com fixtures Markdown/HTML,
  canonicalização, links não-http, encoding e budgets.
- Regressão core e documentação passaram no candidato
  `93bb8894d816aad3c3b3682ccec317db1da39d45`; o gate registrou 22/22 etapas,
  com `499 passed, 10 skipped` na suíte principal isolada (Python 3.11, sem
  `chromadb`), `504 passed, 5 skipped` no venv do projeto (Python 3.14) e zero
  falhas/etapas não executadas.

## Limites

Browser JavaScript, autenticação e assets profundos permanecem fora do
escopo, conforme o ticket; não há aceite externo pendente específico deste
adapter.
