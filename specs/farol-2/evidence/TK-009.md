# TK-009 evidence — ingestão de repositório por escopo

Status: `verified`; seleção documental/código e locators estão implementados.

## Entregue

- `docops/extractors/repository.py` fixa commit, rejeita submódulos e symlinks
  perigosos, aplica budgets e inclui docs/contratos/schemas por padrão.
- Código só entra por `code_include`/`include_code` explícito; símbolos Python
  recebem caminho, linha e locator reconstruível.

## Verificação

- `tests/test_repository_extractor.py` passou com default, opt-in, glob,
  commit, symlink, submodule e budgets.
- Regressão core, `diff --check`, wheel e clone limpo passaram.

## Limites

Não há bloqueio local conhecido; formatos de análise semântica/compilação de
código permanecem fora do ticket.
