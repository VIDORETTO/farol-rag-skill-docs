# TK-017 evidence — remoção do desvio editorial

Status: `verified`; a remoção contratual foi executada agora, sem
`cutover_approved`, porque TK-017 não remove o backend legado (isso é TK-019) e
o ADR-0003 já autoriza a quebra de contrato. A narrativa anterior que dizia que
TK-017 esperava TK-018 estava errada: o que espera cutover é apenas TK-019.

## Entregue

- Removido `docops/presets/mercado-livre.json` e os aliases
  `mercado-livre`/`mercadolivre`/`ml` em `docops/master.py::load_project_preset`;
  `neutral`/`generic`/`neutro` permanecem.
- Removidos `course`/`page`/`offer` de `docops/master.py` (`_KIND_TO_CONTRACT`,
  `_CHANGE_TYPES`, `_NODE_KINDS`, `_deliverables`, `_pending_questions`,
  `_is_valid_price`, criação de artefatos no finalize, `course_edit`/`page_edit`,
  propagação de audiência e `validate_project_derivatives`/
  `prepare_project_derivatives`), de `docops/contracts.py` e dos exports/CLI em
  `docops/__init__.py` e `docops/__main__.py` (incluindo `project-derivatives`).
- Removidos `schemas/course.schema.json`, `schemas/page.schema.json` e as cópias
  empacotadas em `docops/schemas/`; enums de `brief`, `init-session` e
  `project-revision` reduzidos a `knowledge`. `page` permanece como locator-kind
  em `docops/ir/core.py`, `docops/extractors/pdf.py` e no migrator.
- `docops/migration.py` continua reconhecendo `course`/`page`/`offer` e reporta
  `editorial_surface_excluded` sem carregar o dado (AC-027).
- Corrigidos `scripts/check_contracts.py` e
  `scripts/run_master_evolution_fixture.py`; casos editoriais removidos de
  `tests/test_master_evolution.py`; documentos/tickets editoriais dedicados
  removidos (`docs/master-evolution/MERCADO-LIVRE-PRESET.md`,
  `tickets/11-*`, `tickets/15-*`) e links corrigidos.

## Auditor de superfície (correção do falso-positivo)

- `docops/release_v2.py` agora enumera a superfície normativa a partir de
  `git ls-files` (`_tracked_surface_files`), em vez de varrer diretórios
  gitignored (o comportamento anterior gerava ~15k findings de `documents/`,
  `.venv`, `artifacts` etc.).
- O padrão editorial não casa mais o locator genérico `page`; ele detecta os
  símbolos do contrato removido (`course`/`page` ids e schemas, `offer.json`,
  `page.offer`, `commercial_authorization`, `_is_valid_price`, a marca
  Mercado Livre). Allowlist restrita a `docops/release_v2.py`,
  `docops/migration.py`, `tests/test_migration_v2.py`,
  `tests/test_v2_contracts.py`, `tests/test_release_v2_surface.py` e
  `specs/farol-2/`.

## Verificação observada

- `python scripts/check_farol_v2_surface.py --root . --surface editorial` →
  `ok=true`, `337` arquivos varridos, `0` findings.
- O oráculo editorial de AC-028 agora também é exercido contra o checkout real
  no gate (não apenas em `tmp_path`):
  `tests/test_release_v2_surface.py::test_repository_checkout_has_no_editorial_surface_residual`.
  Controle positivo observado: injetar `_LEAK = 'course_id'` em
  `docops/runtime.py` muda `ok` de `True` para `False` (1 finding) e a remoção
  volta a `True`, provando que o oráculo não é vacuoso.
- O caminho de auditoria do **wheel** (nomeado por AC-028) ganhou cobertura: um
  wheel sintético com `docops/presets/course.json` é reprovado e o membro é
  reportado, enquanto um wheel limpo passa
  (`test_wheel_audit_flags_a_removed_editorial_contract`,
  `test_wheel_audit_accepts_a_clean_distribution`). O auditor reporta o termo
  casado, nunca a linha de origem.
- Esses testes são aditivos e sem dependências compartilhadas novas: foram
  verificados focalmente (`tests/test_release_v2_surface.py` → `6 passed`;
  agrupamento focal → `31 passed`) após o gate core de `520 passed`, que
  permanece a medição de código de produto mais recente (nenhum código de
  produção mudou depois dele).
- `--surface editorial --wheel <1.1.0>` → `ok=true`, `140` membros, `0` findings
  (o wheel 1.1.0 de `build/` ainda trazia `course.schema.json`,
  `page.schema.json` e `presets/mercado-livre.json` copiados de um `build/lib`
  obsoleto; limpar o `build/` gitignored e reconstruir remove o resíduo).
- Testes verdes: `tests/test_release_v2_surface.py tests/test_farol_v2_gates.py`
  → `6 passed`; `tests/test_master_evolution.py` → `22 passed`;
  `tests/test_v2_contracts.py tests/test_migration_v2.py tests/test_cli.py`
  → `15 passed`.
- `python scripts/check_contracts.py --json` → `ok=true`, `77` schemas, `0`
  findings; `scripts/sync_schemas.py --check` → `ok=true`, `count=77`.
- `python scripts/check_documentation.py --root . --json` → `ok=true`, `113`
  Markdown, `0` findings.
- `ruff check`/`ruff format --check docops tests scripts` → sem violações.
- `git diff --check` → exit 0.
- Suíte completa local: `502 passed, 5 skipped` (skips: RAGFlow opt-in e quatro
  de symlink indisponível no host).
- Gate core: `python scripts/run_release_gates.py --profile core --timeout 3600
  --output <tmp>` → `ok=true`, `22/22` etapas, `0 failed`, `0 blocked`,
  `0 not_run`; agregado `1078 passed`, `15 skipped`; pytest `502 passed,
  5 skipped`. Report SHA-256
  `6e071075bc39c7f269d51d8a56c11dbc0430193caa28c3dc0ebe05ba10938db8`
  (temporário, não versionado). O `--timeout 3600` foi necessário: a suíte neste
  venv excede o default de `1800s` por comando; sem ele o stage pytest aparece
  `failed/reason=command_timeout`, o que é limite do runner, não defeito do diff.

## Limitações

- Não houve commit nem push (sem autorização). As deleções estão apenas
  staged (`git add -u`), não commitadas; o worktree está dirty.
- Enquanto as deleções estavam unstaged, `git ls-files --cached` ainda listava
  os 8 arquivos removidos e os testes de candidato
  (`tests/test_candidate_*.py`, `test_integration_candidate.py`) reportavam
  “missing file” (10 failures). Com as deleções staged, os mesmos 12 testes
  passam — confirmando que era artefato do índice, não resíduo de contrato.
- A remoção de `knowledge-rag`/Chroma (TK-019) e o dual-run real (TK-018.2–.4)
  continuam `not_run`.
