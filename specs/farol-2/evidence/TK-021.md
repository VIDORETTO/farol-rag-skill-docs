# TK-021 evidence — originais privados e proveniência de release

Status: `verified`; a política de armazenamento privado e o bundle allowlisted
foram cobertos por regressão explícita.

## Entregue

- `docops/source_policy.py` exige rights, privacy, authority e owner explícitos
  antes de reconciliar uma aquisição; snapshots incompletos são preservados e
  não retiram a fonte ativa.
- `docops/release_audit.py` rejeita paths de runtime/corpus, symlinks, segredos
  e caminhos de máquina no conjunto candidato.
- `scripts/prepare_candidate.py` copia apenas assets públicos allowlisted,
  lock/provenance e vendor revisado; não copia originais, `data/`, caches,
  `.docops` ou artefatos de execução.
- `scripts/verify_candidate.py` confirma identidade, digests, checksums,
  provenance independente e `publication.performed=false`.

## Verificação

- `tests/test_candidate_bundle.py::test_candidate_bundle_excludes_private_originals_and_runtime_state`
  passou, verificando um bundle gerado e a ausência dos roots privados/runtime.
- `tests/test_release_audit.py` passou para paths proibidos aninhados, canário
  bearer, symlink/path e auditoria do candidato.
- `tests/test_source_registry.py` passou para direitos/privacidade explícitos,
  preservação de snapshots incompletos e withdrawal autorizado.
- `scripts/check_acceptance_matrix.py --check` passou sem `unattributed` ou
  `backlog_findings`; AC-031 agora é coberto por TK-021.
- O bundle candidate-core de 2026-09-18 foi preparado e verificado
  independentemente; seus artefatos permaneceram temporários e não publicados.

## Limites

Licença e autorização final de cada fonte continuam decisões do mantenedor; a
prova deste ticket garante a fronteira técnica do bundle, não aprova corpus
privado nem executa publicação.
