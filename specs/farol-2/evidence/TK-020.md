# TK-020 evidence — jornada e release Farol 2.0

Status: `verified`; o candidato Farol 2.0 passou a jornada provider-free, a
execução real de `book-to-skill`, RAGFlow/OCR, a contração do legado, wheel,
candidate, documentação, supply-chain e o gate final completo.

## Identidade

- Candidato: `93bb8894d816aad3c3b3682ccec317db1da39d45` na branch `farol-v2`.
- Worktree: dirty, sem commit, push, tag, release ou publicação executados.
- Gate final: `artifacts/release-gates-full-20260920-final7/release-gates.json`.
- SHA-256 do relatório: `EFFE3B18D009CC659326C3F58F7B43FCABBC75540945F7F55B23EEBD0FD29824`.

## Entregas

- `scripts/run_farol_v2_fixture.py` cobre projeto → fontes → IR → taxonomia →
  skills → router → evidência → composição → rollback.
- `scripts/run_book_to_skill_profile.py` executa o harness externo instalado,
  recebe somente projeções IR aprovadas e valida skills/chapters/glossary/
  patterns/cheatsheet com lineage canônico.
- `scripts/run_release_gates.py` possui perfis core, book-to-skill, RAGFlow e
  OCR; o perfil `full` executa todos os gates sem skip silencioso.
- O wheel e o candidate bundle excluem corpus privado, caches, tokens, estado
  de runtime e superfícies legadas. A publicação permanece manual.

## Verificação terminal — 20 de setembro de 2026

Comando observado:

```text
.venv-rag\Scripts\python.exe scripts/run_release_gates.py --root . --python .venv-rag\Scripts\python.exe --profile full --timeout 3600 --output artifacts/release-gates-full-20260920-final7 --json
```

Resultado: `ok=true`, `25/25` etapas, `1045 passed`, `12 skipped`, `0 failed`,
`0 blocked`, `0 not_run`.

Resumo dos gates:

- suíte principal: `474 passed, 6 skipped`;
- clean clone: `474 passed, 6 skipped`;
- security: `26 passed`;
- crash matrix: `17 passed`;
- revogação: `25 passed`;
- book-to-skill: `passed`, duas skills e validação/scan externos verdes;
- RAGFlow: `2 passed`, SDK `0.27.2`, runtime fixado por digest;
- OCR: `1 passed`, Docling `2.129.0`, ONNX Runtime `1.30.0`, RapidOCR;
- contratos: `77` schemas, sem findings;
- documentação: `164` Markdown, sem findings;
- surface `legacy/all`: zero findings;
- wheel core, candidate e supply-chain: verificados.

Os seis skips são explícitos: três integrações externas opt-in repetidas na
suíte principal/clone e três testes de symlink indisponível no host. O gate
não converteu nenhum deles em sucesso; as integrações reais passaram em seus
perfis dedicados.

## Estado final

Todos os 33 critérios de aceite estão `verified`. TK-001–021 estão verificados,
incluindo TK-018 (`cutover_approved`), TK-019 (remoção do legado) e TK-021
(originais privados/provenance). O único passo restante é decisão humana sobre
commit, push, tag, release ou publicação; nenhuma implementação permanece.
