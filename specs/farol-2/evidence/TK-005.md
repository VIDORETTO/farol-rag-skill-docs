# TK-005 evidence — registry governado de extractors

Status: `verified`; implementação local e os aceites do ticket estão verdes.

## Entregue

- `docops/extractors/registry.py` seleciona por hints, capability, assinatura,
  política e prioridade nativa, com fallback legado explícito.
- Plugins remotos/terceiros são bloqueados antes de receber bytes sem opt-in;
  doctor expõe capacidades sem caminhos ou segredos.

## Verificação

- `tests/test_extractor_registry.py` passou com seleção de diretório,
  fallback, opt-in, budgets e redaction.
- No candidato `93bb8894d816aad3c3b3682ccec317db1da39d45`, o gate core passou
  22/22 etapas; a suíte principal registrou `499 passed, 10 skipped` no
  ambiente core isolado (Python 3.11, sem `chromadb`) e `504 passed, 5 skipped`
  no venv do projeto (Python 3.14, com `chromadb`), sem falhas ou etapas não
  executadas.
- `scripts/check_public_seams.py --tests tests --json` passou após classificar
  os fixtures de seam.

## Limites relacionados

O receipt externo TK-001 continua necessário para tickets que dependem de
RAGFlow, mas não é requisito de aceite do registry governado deste ticket.
