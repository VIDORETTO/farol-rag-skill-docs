# TK-016 evidence — migração 1.x para candidata 2.0

Status: `verified`; inspect/plan/apply/rollback provider-free implementados.

## Entregue

- `docops/migration.py` inspeciona/hash-eia origem, produz plano com itens
  importados/excluídos e direitos desconhecidos, sem importar Chroma/data.
- Apply usa staging, valida `knowledge-project-v2`, detecta fonte stale e
  rollback move a candidata para backup recuperável sem mutar a origem.
- CLI expõe `v2 start/inspect/apply` e aliases de compatibilidade.

## Verificação

- `tests/test_migration_v2.py` e `tests/test_project_v2.py` passaram para
  dry-run, exclusões editoriais, stale source, idempotência, CLI e rollback.
- Contratos, wheel core e clone limpo passaram no release gate.

## Limites relacionados

O resultado é uma candidata segura; promoção como substituição operacional só
é permitida depois de cutover aprovado por TK-018.
