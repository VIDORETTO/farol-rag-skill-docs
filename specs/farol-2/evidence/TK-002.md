# TK-002 evidence — contratos fundamentais v2

Status: `verified`.

## Executed procedures

- `/root/.hermes/motokao/.venv/bin/python -m pytest -q tests/test_v2_contracts.py`
  → `4 passed`.
- `/root/.hermes/motokao/.venv/bin/python -m pytest -q tests/test_v2_contracts.py tests/test_post_contracts.py tests/test_public_interface.py`
  → `17 passed`.
- `uv run --isolated --python 3.11 --with PyYAML==6.0.3 python scripts/sync_schemas.py --check`
  → `ok`, 79 canonical/bundled schemas byte-synchronized.
- `uv run --isolated --python 3.11 --with PyYAML==6.0.3 python scripts/check_contracts.py --json`
  → `ok`, all 79 registered contracts including the five v2 artifacts.

## Delivered

- Strict `knowledge-project-v2`, `operation-v2`, `operation-result-v2`,
  `capability` and `migration-v2` schemas under `schemas/` and synchronized
  copies under `docops/schemas/`.
- Frozen public Python envelopes in `docops.api_types` and root exports.
- `validate_contract` distinguishes schema-version, compare-and-swap revision
  and idempotency payload conflicts; 1.x `validate_artifact` behavior remains.
- Migration envelopes carry source identity and excluded-item reasons; no
  migration code mutates a source package in this ticket.

Environment note: the checkout has no `python` alias; the equivalent isolated
Python 3.11 environment was used explicitly. The release core profile later
passed the full collection with pinned test/build dependencies.
