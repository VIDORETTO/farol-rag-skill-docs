# TK-007 evidence — PDF textual e OCR autorizado

Status: `verified`; textual, quarentena, policy e OCR local real foram
executados.

## Entregue

- `docops/extractors/pdf.py` extrai por página com `pypdf`, preserva locators e
  distingue PDF textual de escaneado.
- Escaneado sem opt-in entra em quarentena; OCR injetado exige autorização
  remota, capability `ocr` e limiar de confiança.

## Verificação

- `tests/test_pdf_extractor.py` passou com páginas distinguíveis, OCR
  bloqueado/autorizado, confiança, payload e budgets.
- A suíte completa passou; o único skip ligado ao RAGFlow é o spike opt-in.

### Endurecimento provider-free — 2026-09-19

- O extractor agora diferencia PDF malformado (`malformed`) e PDF criptografado
  (`encrypted`) de um PDF válido sem camada textual (`ocr_required`), evitando
  encaminhar bytes inválidos ao OCR remoto.
- Saída OCR com confiança não finita ou fora de `0..1` falha como
  `ocr_output_invalid`; `NaN` não pode mais atravessar o limiar e produzir IR.
- Locators OCR exigem páginas inteiras positivas; vários blocos na mesma página
  permanecem válidos e página zero/fracionária é recusada antes da IR.
- RED observado: o PDF malformado era marcado como `quarantined/ocr_required` e
  confiança `NaN` produzia documento ativo.
- GREEN focal: `tests/test_pdf_extractor.py` → `7 passed`; regressão relacionada
  (`test_pdf_extractor`, `test_normalizer`, `test_extractor_registry`,
  `test_formats_portuguese`) → `25 passed`.

## Execução OCR real — 2026-09-20

- `docling==2.129.0`, `onnxruntime==1.30.0` e RapidOCR processaram PDF sintético
  escaneado de duas páginas pelo `PdfExtractor`/`DoclingOcrAdapter` real.
- `run_ocr_profile.py --timeout 900 --json` → `status=passed`, `1 passed`, sem
  skip/falha; texto das duas páginas, page/bbox, confiança `>=0.8`, execução
  local e `quality_flags=[ocr]` foram observados.
- O original não foi enviado remotamente e o receipt redigido não contém texto
  marcador nem caminho privado.
