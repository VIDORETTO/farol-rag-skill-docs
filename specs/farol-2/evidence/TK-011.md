# TK-011 evidence — síntese multi-skill e lineage

Status: `verified`; contrato/engine local e execução real do harness
`book-to-skill` foram verificados em 20 de setembro de 2026.

## Entregue

- `docops/synthesis.py` valida budgets, owners, idioma, claims e lineage por
  hash/locator da IR, com receipts idempotentes e layout multi-skill.
- O adapter de contrato `BookToSkillAdapter` permite a fixture provider-free
  sem chamar modelo ou escolher provedor.

## Verificação

- `tests/test_synthesis.py` passou para receipts, lineage, budget, owner e
  idempotência.
- `scripts/run_farol_v2_fixture.py` passou no gate core sem expor corpus,
  segredo ou saída de modelo.

### Endurecimento provider-free — 2026-09-19

- Lineage de uma skill agora só pode referenciar blocos incluídos na projeção
  aprovada daquele tópico; referência cruzada fora do escopo é rejeitada.
- Slug devolvido pelo harness deve preservar o slug da taxonomia aprovada e
  nomes de capítulos ficam restritos a arquivos Markdown seguros, sem traversal.
- O adapter local registra `execution=contract-fixture`, evitando que uma
  fixture determinística seja confundida com execução externa do harness.
- REDs foram observados para lineage cruzado, slug inseguro, capítulo com
  traversal e receipt ambíguo. Regressão relacionada (`synthesis`, `taxonomy`,
  `pipeline`, `enrichment`) → `31 passed, 1 skipped`; o skip é a capacidade de
  symlink indisponível neste host.

## Execução real terminal — 2026-09-20

- Harness: `book-to-skill`, commit
  `526f362552562d88c1a8bbf8012d2cee93f831d5`, licença MIT.
- Fixture: `documents/fixtures/acme-docs/`, duas fontes Markdown sintéticas,
  131 palavras e aproximadamente 174 tokens extraídos pelo `extract.py` real.
- IR: dois documentos, 11 blocos, revisão
  `f9b570755fe7771ff91c8723b7adf5e4dca06317802d6c46385f6496117fb5b3`;
  a síntese recebeu apenas projeções de blocos por tópico, não os originais.
- Taxonomia aprovada: dois owners únicos (`acme-api-reliability` e
  `acme-api-release`), cobertura 1.0 e um overlap contextual explícito.
- Saída real do executor: duas skills em PT-BR, capítulos, glossary,
  patterns, cheatsheet e sidecars de lineage; 5 claims factuais com 5 refs de
  blocos canônicos.
- Receipt de síntese: request hash
  `1fd57c352475efbb7926a30d2b04a57ad7139ae113f0f5896792473b78cf35a2`, output
  hash `79373f1fd679d3c9483372f6e6a64b18be0db0ebac90b35a43c2adafac98b21c`.
- Validação externa: `validate_skill.py` passou para as duas `SKILL.md`;
  `scan_generated_skill.py` passou para as duas árvores; nenhuma finding de
  injeção ou autoridade foi registrada.
- Perfil reproduzível: `python scripts/run_book_to_skill_profile.py
  --source-dir documents/fixtures/acme-docs --harness-root
  <personal-skill-root>/book-to-skill --output
  artifacts/book-to-skill-tk011-20260920-rerun --json` → `status=passed`.

O output permaneceu somente em `artifacts/`, que é ignorado e não faz parte do
release. O receipt redigido registra hashes, contagens e identidade do harness,
sem copiar corpus bruto ou caminhos privados para a evidência distribuível.

## Pendente

Nenhuma pendência de TK-011 permanece. A próxima dependência é o dual-run real
de TK-018 com os mesmos candidato, IR, Golden, filtros e contexto nos braços
legado e RAGFlow.
