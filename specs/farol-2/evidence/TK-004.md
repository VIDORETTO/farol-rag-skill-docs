# TK-004 evidence — IR canônica

Status: `verified`; a fatia local e a dependência de locator/mapping real do
RAGFlow foram executadas.

## Entregue

- `docops/ir/` materializa `IRDocument`, `IRBlock`, `ExtractionReceipt` e
  `BackendMapping` com hashes, locators, budgets e escrita atômica.
- Schemas canônicos e empacotados foram sincronizados; blocos estruturados não
  são reduzidos silenciosamente a texto.

## Verificação

- `tests/test_ir.py` passou, cobrindo invariantes, hash, parent/ordinal,
  atomicidade, idempotência e mapping.
- `scripts/sync_schemas.py --write` e `scripts/check_contracts.py --json`
  passaram para os 79 contratos registrados naquele momento. TK-017 depois
  removeu `course`/`page`, reduzindo o total para 77; a contagem atual está em
  `evidence/TK-017.md` e no gate core mais recente.
- Os contratos IR agora também têm fixtures positivas no gate
  (`ir-block`, `ir-document`, `extraction-receipt` em
  `scripts/check_contracts.py`), exercitando a validação de payload real de
  AC-004/AC-009 — antes só as regras estruturais compartilhadas eram checadas
  para esses três. `check_contracts.py --json` → `ok=true`, `0` findings.
- No artefato histórico do baseline `81d5dcb2e189d00406cdd9b9e671d94e3f23cd58`,
  o release gate core registrou 22/22 etapas, com 459 passados e 6 skips
  explícitos; a integração RAGFlow real não faz parte desse perfil. Essa
  contagem não é atribuída automaticamente ao candidato atual
  `93bb8894d816aad3c3b3682ccec317db1da39d45`.
- No candidato `93bb8894d816aad3c3b3682ccec317db1da39d45`, o release gate core
  final passou 22/22 etapas com `0 failed`, `0 blocked` e `0 not_run`; a suíte
  principal isolada (Python 3.11, sem `chromadb`) registrou `499 passed,
  10 skipped` e o venv do projeto (Python 3.14) `504 passed, 5 skipped`.

## Fechamento da dependência externa — 2026-09-20

O spike TK-001 observou chunks reais com locators de página e o adapter real
reconstruiu mapping para IDs canônicos, incluindo rebuild com IDs externos
novos e projeção canônica equivalente. A limitação externa anterior não está
mais pendente.

## Endurecimento provider-free — 2026-09-19

- O validator da IR agora exige ordinais contíguos na mesma ordem do documento,
  confiança finita em `0..1` e ranges válidos para `line`, `page`, `slide`,
  `timestamp` e `bbox`.
- O grafo de `parent_id` passou a rejeitar ciclos, além das referências
  pendentes que já eram recusadas.
- REDs foram observados para ordinal fora da sequência, confiança `NaN`, linha
  zero e ciclo de parent. A regressão de IR, contratos e extractors terminou
  com `38 passed`.
