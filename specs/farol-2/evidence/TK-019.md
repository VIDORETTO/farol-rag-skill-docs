# TK-019 evidence — guard para remoção do legado

Status: `verified`; a contração de `knowledge-rag`/Chroma foi executada somente
depois do receipt atual `cutover_approved` e verificada em 20 de setembro de
2026.

## Correção de narrativa

A nota anterior sugeria que a contração editorial de TK-017 também esperava
cutover. Isso estava errado: TK-017 removeu o desvio editorial com base no
ADR-0003 (ver [TK-017](TK-017.md)). Apenas TK-019 (backend `knowledge-rag` e
Chroma) exige `cutover_approved` atual, porque é o que rompe paridade e
rollback.

## Entregue

- Auditoria de superfície e dependências expõe caminhos/linhas/termos apenas,
  sem publicar conteúdo; migration/specs podem ser explicitamente allowlisted.
- O auditor agora varre apenas arquivos git-tracked em escopo normativo; não
  polui o oráculo com `documents/`, caches ou artefatos gerados.
- O guard `require_cutover_approved()` exige decisão exata `cutover_approved`
  com `ok=true` antes de qualquer lote destrutivo.

## Verificação

- `python scripts/check_farol_v2_surface.py --root . --surface legacy` reporta
  os caminhos legados esperados (ex.: `docops/backends/legacy_knowledge_rag.py`,
  `docops/rag_sync.py`, `config.yaml`) — esta é a superfície que TK-019 removerá
  após o cutover, não um motivo para agir agora.
- Testes de superfície e de gate de cutover passam; nada de `knowledge-rag`/
  Chroma foi removido.

### Verificação atual — 2026-09-18

- `scripts/check_farol_v2_surface.py --root . --surface editorial` →
  `ok=true`, `337` arquivos, `0` findings; contratos e matriz de aceite também
  passaram (`findings=[]`, `backlog_findings=[]`).
- O gate core final passou `22/22` estágios com `0` falhas e `0` not_run,
  incluindo clean clone, wheel core, compileall, diff-check, crash matrix e
  revogação. A remoção continua deliberadamente não executada porque o
  dual-run real está bloqueado e não existe `cutover_approved` atual.

## Pendente

Nenhuma pendência de TK-019 permanece. TK-020 ainda precisa fechar a jornada
de release final, perfis externos e handoff sem publicação.

## Execução terminal — 2026-09-20

- Receipt exigido: `cutover_approved`, candidato
  `93bb8894d816aad3c3b3682ccec317db1da39d45`, receipt hash
  `287c83864f37adfd9d84486aeb3da2061350110ec2a40160458802247e153301`.
- Removidos do produto 2.0: adapter/runtime do backend legado, sincronizador
  MCP, vendor, scripts de smoke/reindex/atualização legados, dependência
  opcional e lock direto do backend antigo, além da configuração local que
  apontava para o índice antigo.
- Preservados: contratos de migração 1.x, receipt histórico do dual-run,
  governança de fontes, snapshots genéricos, reader local e rollback de
  pacotes. Nenhum corpus, cache, token ou estado de runtime foi tocado.
- A configuração/harness agora descreve somente o adapter externo RAGFlow
  v0.27.2; indexação sem perfil/credencial externo falha fechada como operação
  não executada, não como sucesso.
- Auditoria `python scripts/check_farol_v2_surface.py --root . --surface
  legacy` → `ok=true`, zero findings, 314 arquivos normativos escaneados.
- Auditoria de contratos → `ok=true`, 77 schemas; documentação → `164` Markdown,
  zero findings; Ruff e compileall passaram.
- Regressão local terminal → `474 passed, 6 skipped`; os skips são integrações
  externas opt-in e symlink indisponível no host.
- Bundle/candidate, supply-chain e migration tests passaram sem vendor,
  dependency ou cache privado no candidato.
