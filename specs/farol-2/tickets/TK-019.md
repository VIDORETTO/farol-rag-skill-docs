---
schema: hybrid/ticket
schema_version: "1.0"
id: TK-019
effort: farol-2
type: migration
status: verified
ticket_revision: 1
requires: [TK-018]
requirement_refs: [FR-010, FR-014, FR-017]
acceptance_refs: [AC-021, AC-027, AC-029]
spec_revision: 1
plan_revision: 1
owned_areas: [docops/backends, docops/runtime.py, docops/rag_sync.py, skills/vendor/knowledge-rag, pyproject.toml, requirements.lock, config.yaml]
verification_status: passed
---

# TK-019 — Remover knowledge-rag e Chroma do produto 2.0

## Objetivo e limites

Após cutover aprovado, remover adapter/vendor/dependências/configuração/scripts e
contratos específicos do legado, mantendo migrator/rollback de pacotes 1.x.

Não inclui: apagar pacotes 1.x do usuário ou histórico Git.

## Leitura em ordem

1. Receipt TK-018 → autorização de cutover.
2. `skills/vendor/knowledge-rag/PROVENANCE.json` → inventário do vendor.
3. `docops/runtime.py`, `rag_sync.py`, `retrieval.py`, `harness.py` → acoplamentos.
4. `pyproject.toml`, `requirements.lock`, `docs/DEPENDENCIES.md`.

## Decisões já resolvidas

- Só executar com `cutover_approved` atual e mesmos inputs.
- RAGFlow é único backend de novos projetos.
- Migração 1.x reporta requisito antigo sem instalar/ativar legacy.

## Mapa de alterações

- Excluir: adapter legado, vendor, skills rag específicas e config Chroma.
- Alterar: runtime/rag_sync/retrieval/harness/deps/release/docs/tests.
- Fora: dados locais ignorados e pacotes externos do usuário.

## Contrato técnico

- Entradas: checkout com receipt/digests válidos.
- Saídas: wheel 2.0 sem legado; RAGFlow continua reconstruível; migrator legível.
- Invariantes: não tocar `data/`, `models_cache/`, corpus ou env global.
- Erros: stale cutover, residual dependency/symbol/package, migration regression.
- Efeitos: somente arquivos versionados explicitamente inventariados.

## Exemplos de aceite

- **AC-029**: auditor não encontra dependency/vendor/runtime selectable; fluxo v2 passa.
- **AC-027**: pacote 1.x ainda gera plano de migração sem iniciar Chroma.
- **AC-021**: apagar/recriar índice RAGFlow continua possível da IR.

## Dependências e sequência de execução

Depende de: TK-018 done com cutover aprovado.

- [x] TK-019.1 Auditor RED de dependências/símbolos/artefatos legados.
- [x] TK-019.2 Remover consumers/deps/config em lotes mantendo testes verdes.
- [x] TK-019.3 Remover vendor/skills/docs e atualizar supply chain.
- [x] TK-019.4 Wheel limpo, migração e gates core/RAGFlow.

## Validação

- Comandos: auditor dedicado; `python -m pytest -q`; `python -m pip check`; gates core/ragflow; candidate bundle audit.

## Condição de retorno à planejadora

Retornar se receipt estiver stale, rollback depender do runtime legado ou RAGFlow
não passar no mesmo SHA.

## Relatório de saída

Relatar paths removidos, dependências, auditor, migração, wheel e gates.
