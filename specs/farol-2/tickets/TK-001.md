---
schema: hybrid/ticket
schema_version: "1.0"
id: TK-001
effort: farol-2
type: research
status: verified
ticket_revision: 1
requires: []
requirement_refs: [FR-005, FR-010, FR-017]
acceptance_refs: [AC-006, AC-019, AC-020]
spec_revision: 1
plan_revision: 1
owned_areas: [tests/spikes, specs/farol-2/evidence]
verification_status: passed
---

# TK-001 — Provar os seams essenciais do RAGFlow fixado

## Objetivo e limites

Executar um spike limitado contra RAGFlow v0.27.2 fixado por digest e registrar
se dataset, upload, parsing, exportação de chunks/locators, retrieval e cleanup
sustentam H-001/H-002.

Não inclui: adapter de produção, mudança do backend ativo ou benchmark de carga.

## Leitura em ordem

1. `specs/farol-2/research.md` → RAGFlow escolhido — versão e claims permitidos.
2. `specs/farol-2/data-model.md` → IRBlock — locators que o spike precisa observar.
3. `docops/rag_sync.py` → `RagSynchronizer.sync` — lifecycle legado a comparar.
4. `tests/test_mcp_smoke.py` — padrão de integração externa e skips explícitos.

## Decisões já resolvidas

- Usar HTTP/SDK para lifecycle; chat/agente do RAGFlow está fora do escopo.
- Fixar imagem e SDK por digest; liberdade local: escolher fixture sintética pequena.
- Falha da hipótese retorna ao plano; não reduzir silenciosamente a IR.

## Mapa de alterações

- Novo: `tests/spikes/test_ragflow_contract.py` — prova opt-in real.
- Novo: `specs/farol-2/evidence/ragflow-spike.md` — resultados e limitações.
- Fora: `docops/` e backend ativo — não alterar.

## Contrato técnico

- Entradas: endpoint/token por env, imagem v0.27.2 por digest, fixtures PDF/DOCX.
- Saídas: receipt com operações, IDs redigidos, estrutura/locators observados.
- Invariantes: nenhum segredo/corpus no repositório; cleanup apenas do dataset criado.
- Erros: `not_run` se recurso ausente; `failed_hypothesis` se operação/locator faltar.
- Efeitos: dataset temporário com prefixo aleatório validado antes de remover.

## Exemplos de aceite

- **AC-019/020**: instância saudável + fixture → dataset isolado, parse terminal,
  retrieval e cleanup; token nunca aparece. Oráculo: conteúdo/locator conhecido da fixture.
- **AC-006**: PDF de duas páginas → blocos distinguíveis por página ou evidência
  de que um extractor complementar é obrigatório.

## Dependências e sequência de execução

Depende de: nenhum.

- [x] TK-001.1 Fixar runtime e escrever contract test opt-in; observar red esperado.
- [x] TK-001.2 Provar dataset/upload/parse/retrieve/cleanup e locators.
- [x] TK-001.3 Registrar capabilities, lacunas, recursos e hashes.
- [x] TK-001.4 Executar cleanup e comprovar ausência de resíduo conhecido.

## Validação

- Diretório: raiz.
- Comando: `DOCOPS_RAGFLOW_INTEGRATION=1 python -m pytest -q tests/spikes/test_ragflow_contract.py`.
- Esperado: passed ou skip/not_run explícito por capacidade; nunca falso verde.
- Distinguir ambiente: conexão/auth/resource é bloqueio externo; resposta incompatível é hipótese falha.

## Condição de retorno à planejadora

Retornar se RAGFlow não expuser estrutura/locator suficiente, a API documentada
divergir ou o digest não puder ser fixado.

## Relatório de saída

Relatar versão/digest, ambiente, operações, locators, recursos, resultados e
decisão sobre H-001/H-002.
