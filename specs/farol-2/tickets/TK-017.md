---
schema: hybrid/ticket
schema_version: "1.0"
id: TK-017
effort: farol-2
type: migration
status: verified
ticket_revision: 1
requires: [TK-002, TK-016]
requirement_refs: [FR-014]
acceptance_refs: [AC-027, AC-028]
spec_revision: 1
plan_revision: 1
owned_areas: [docops/master.py, docops/contracts.py, docops/__init__.py, docops/__main__.py, schemas, docs, tests]
verification_status: passed
---

# TK-017 — Remover Mercado Livre, curso, página e oferta

## Objetivo e limites

Contrair o contrato 2.0 removendo runtime, schemas, CLI, exports, presets, testes
e documentação normativa do desvio editorial, após existir migração segura.

Não inclui: remover governança, fontes, claims, conflitos, presets genéricos,
readers, candidatas, backup ou histórico Git.

## Leitura em ordem

1. `docs/adr/0003-focar-farol-em-conhecimento-para-agentes.md`.
2. `docops/master.py` → `_deliverables`, init/derivatives/preset aliases.
3. `docops/contracts.py`, `docops/__init__.py`, `docops/__main__.py` → seams públicos.
4. `schemas/` + `docops/schemas/` e `tests/test_master_evolution.py`.

## Decisões já resolvidas

- Remoção é breaking 2.0; histórico fica no Git.
- Preservar capacidades genéricas de conhecimento.
- Não manter aliases que fingem feature desabilitada.

## Mapa de alterações

- Excluir: preset/documento/ticket Mercado Livre dedicados; schemas course/page.
- Alterar: master/contracts/exports/CLI/tests/fixtures/docs/package data.
- Fora: `specs/farol-2/` e migration report que menciona exclusão historicamente.

## Contrato técnico

- Entradas: checkout após TK-016 e inventário de símbolos/strings.
- Saídas: wheel/core sem interfaces excluídas; migration ainda reconhece/report.
- Invariantes: schemas sincronizados; nenhuma referência normativa/packaged residual.
- Erros: checker falha em string/symbol/package proibido fora allowlist de migração.
- Compatibilidade: 1.x somente via migrator/read-only, não exports 2.0.

## Exemplos de aceite

- **AC-028**: auditor do wheel não encontra Mercado Livre/course/page/offer;
  projeto v2 continua criar fonte/taxonomia/skill/RAG.
- **AC-027**: migrator reporta campo excluído sem carregá-lo no projeto.

## Dependências e sequência de execução

Depende de: TK-002 e TK-016 done.

- [x] TK-017.1 Criar auditor RED de superfície proibida com allowlist de migração.
- [x] TK-017.2 Remover preset/aliases/docs dedicados e adaptar fixture neutra.
- [x] TK-017.3 Remover contratos/schemas/runtime/editorial por consumidores.
- [x] TK-017.4 Sincronizar docs/schemas, wheel e regressão completa.

## Validação

- Comandos: `rg -n -i 'mercado[ -]livre|mercadolivre|course|page|offer' docops schemas skills docs tests scripts`; auditor dedicado; `python -m pytest -q`; `python scripts/run_release_gates.py --profile core --output <tmp>`.
- Interpretação: ocorrência permitida apenas no migrator/changelog explicitamente allowlisted.

## Condição de retorno à planejadora

Retornar se algum contrato editorial possuir consumidor de conhecimento não
representado em v2 ou se remoção exigir apagar histórico/dado de origem.

## Relatório de saída

Relatar arquivos removidos/generalizados, allowlist, migração, wheel e regressões.
