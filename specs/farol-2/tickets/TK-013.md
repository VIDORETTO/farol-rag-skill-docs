---
schema: hybrid/ticket
schema_version: "1.0"
id: TK-013
effort: farol-2
type: delivery
status: verified
ticket_revision: 1
requires: [TK-001, TK-003, TK-004]
requirement_refs: [FR-010, FR-015]
acceptance_refs: [AC-019, AC-020, AC-021, AC-022, AC-023]
spec_revision: 1
plan_revision: 1
owned_areas: [docops/backends/ragflow.py, docops/harness.py, docops/doctor.py, config/ragflow]
verification_status: passed
---

# TK-013 — Implementar lifecycle RAGFlow isolado

## Objetivo e limites

Adicionar `RagFlowAdapter` para probe, dataset de candidata, upload/parse/poll,
stats, snapshot, discard e close, com configuração segura e Compose dev opcional.

Não inclui: qualidade final de query/citação ou cutover.

## Leitura em ordem

1. Evidência TK-001 → operações/capabilities reais.
2. `docops/backends/base.py` → Interface criada por TK-003.
3. `docops/harness.py` → manifest MCP fixo atual.
4. `docops/config_audit.py` → regras de transporte/segredo.

## Decisões já resolvidas

- HTTP/SDK para lifecycle; dataset por candidata; IDs em mapping.
- HTTPS ou loopback dev; token somente env/secret ref.
- Compose não inicia por import/plan/core test.

## Mapa de alterações

- Novo: `docops/backends/ragflow.py`, config example/Compose dev, contract
  tests e runner de perfil.
- Existente: generalizar harness/doctor/config audit/schema.
- Fora: remover legacy/vendor.

## Contrato técnico

- Entradas: backend config, project/IR revision, originals/projections autorizados.
- Saídas: candidate/index receipt, capabilities/stats/mapping.
- Invariantes: isolamento; segredo redigido; deadline/cancel; source hash metadata.
- Erros: unavailable/auth/tls/protocol/parse timeout/quota.
- Idempotência: reconcile por chave canônica; cleanup só de objeto pertencente.

## Exemplos de aceite

- **AC-019–023**: fake prova protocolo/erros; instância real prova lifecycle e
  reconstrução; indisponível deixa candidata retomável e não ativa.

## Dependências e sequência de execução

Depende de: TK-001, TK-003 e TK-004 done.

- [x] TK-013.1 Contract tests RED para happy/error/idempotency.
- [x] TK-013.2 Implementar cliente/probe/config segura.
- [x] TK-013.3 Implementar dataset/upload/parse/poll/discard.
- [x] TK-013.4 Provar integração real opt-in; core sem Docker passou.
  - [x] Compose dev opt-in fixado por commit/digest e limitado a loopback;
    execução real permanece condicionada ao runtime externo.

## Validação

- Comandos: `python -m pytest -q tests/test_ragflow_backend.py`;
  `DOCOPS_RAGFLOW_INTEGRATION=1 python -m pytest -q
  tests/spikes/test_ragflow_contract.py`; `python scripts/run_ragflow_profile.py
  --json`; `python -m docops config-audit config/ragflow/example.yaml --json`.

## Condição de retorno à planejadora

Retornar em drift de API, impossibilidade de ownership/cleanup ou segredo exigido no manifest.

## Relatório de saída

Relatar digest/runtime, lifecycle, retries, cleanup, recursos e limites.
