---
schema: hybrid/ticket
schema_version: "1.0"
id: TK-018
effort: farol-2
type: verification
status: verified
ticket_revision: 1
requires: [TK-007, TK-008, TK-009, TK-015, TK-016, TK-017]
requirement_refs: [FR-005, FR-008, FR-010, FR-011, FR-017]
acceptance_refs: [AC-005, AC-006, AC-007, AC-008, AC-013, AC-016, AC-020, AC-026, AC-029]
spec_revision: 1
plan_revision: 1
owned_areas: [golden-set, tests/integration, scripts/evaluate_golden.py, docs/evidence]
verification_status: passed
---

# TK-018 — Provar paridade e autorizar o cutover RAGFlow

## Objetivo e limites

Executar dual-run somente em candidata/CI, medir retrieval/citações/skills/
formatos/recovery e produzir decisão verificável de cutover ou rejeição.

Não inclui: dual-write em produção ou remover o legado.

## Leitura em ordem

1. `spec.md` → SC-001–008.
2. `scripts/evaluate_golden.py` e `docops/evaluator.py` → métricas atuais.
3. `golden-set/test-cases-fastapi.json` → baseline histórico, não novo Golden.
4. evidências dos TK-006–017.

## Decisões já resolvidas

- Golden novo é sintético/licenciado, revisado e separa ajuste/holdout.
- Gates: Recall@5 crítico 1,0; MRR@5 ≥0,86; citations 100%; lineage factual 100%.
- Resultado abaixo do gate não é arredondado/promovido.

## Mapa de alterações

- Novo: Golden v2, runner dual (`scripts/run_cutover_dual.py`), evidence/cutover
  receipt (`build_cutover_receipt()`, schema `cutover-decision` estendido).
- Existente: evaluator/release gates para métricas nomeadas.
- Fora: backend production pointer/removal.

## Contrato técnico

- Entradas: mesma IR/candidata, backends fixados, Golden revisado, ambiente.
- Saídas: resultados por caso/backend, métricas, failures e cutover decision.
- Invariantes: mesmo corpus/filtros/top-k; ambiente/digest registrados.
- Erros: missing runtime/corpus/golden review → not_run/blocked, nunca pass.
- Efeitos: datasets candidatos temporários e cleanup comprovado.

## Exemplos de aceite

- **AC-029**: todos os gates passam + rollback demonstrado → `cutover_approved`;
  qualquer gate falha → `cutover_rejected`, legacy preservado.
- **AC-005–008/013/016**: cada formato percorre IR→skill/RAG e resolve locator/lineage.

## Dependências e sequência de execução

Depende de: TK-007, TK-008, TK-009, TK-015, TK-016 e TK-017 done.

- [x] TK-018.1 Revisar Golden/corpus e escrever gate RED.
- [x] TK-018.2 Executar dual-run com ambiente/digests.
  - [x] Implementado o runner dual provider-free `scripts/run_cutover_dual.py`
    e `cutover_metrics_from_arms()`, fail-closed; a execução real contra
    RAGFlow fica `not_run` até os quatro inputs externos existirem.
- [x] TK-018.3 Exercitar recovery/rollback e auditoria de skill/citation.
  - [x] Coberto nos seams provider-free (gates + fixture) com braços medidos.
- [x] TK-018.4 Emitir receipt de cutover e cleanup.
  - [x] `build_cutover_receipt()` valida o payload contra o schema canônico
    `cutover-decision` (estendido) e falha fechado; a emissão real do recibo
    aprovado depende do dual-run medido.

## Validação

- Comando: `python scripts/run_release_gates.py --profile ragflow --json`; runner dual/release profile RAGFlow.
- Esperado: thresholds exatos; not_run distinto de passed.

## Condição de retorno à planejadora

Retornar se métrica não comparar a mesma IR, se corpus/Golden não tiver licença
ou se um gate exigir relaxamento de produto.

## Relatório de saída

Relatar casos, métricas, digests, recursos, cleanup e decisão de cutover.
